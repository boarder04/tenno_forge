// Empty seed — fresh visitors start with no items, builds, mods or arcanes.
// Use the Export / Import / Merge buttons in the footer to share build packs.
export function seedAll() {
  return { items: [], builds: [], mods: [], arcanes: [] };
}
